# GPU-нода: HTTP-обёртка над ai-sage/GigaAM-Multilingual.
# Версии сверены живьём с PyPI/download.pytorch.org 28.07.2026 — torch/torchaudio
# собраны под CUDA 12.6 (совпадает с базовым образом gpu-node/Dockerfile).
--extra-index-url https://download.pytorch.org/whl/cu126

torch==2.10.0+cu126
torchaudio==2.10.0+cu126

# Рекомендовано карточкой модели ai-sage/GigaAM-Multilingual.
transformers==5.14.1
huggingface_hub==1.25.1
hydra-core==1.3.4
omegaconf==2.3.1
# Импортируется modeling_gigaam.py на уровне модуля (для decoding с
# SentencePiece-токенизатором у других вариантов модели); для revision=ctc
# фактически не используется в рантайме, но без пакета упадёт импорт.
sentencepiece==0.2.2
soundfile==0.14.0
numpy==2.5.1

fastapi==0.140.10
uvicorn[standard]==0.51.0
