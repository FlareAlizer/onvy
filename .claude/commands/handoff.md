---
description: Write a session handoff so the next session (or another agent) continues without re-learning
---
Write `docs/handoff.md`: current goal · state of each open loop (done/in-progress/blocked + verification status) · decisions made this session and why · gotchas discovered · exact next actions (commands, files) · open questions for the user.
Also store the same essentials in ruflo memory (`project/handoff/<date>`). Anti-slop prose: specific, no filler.
