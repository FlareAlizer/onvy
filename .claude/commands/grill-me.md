---
description: Relentless one-question-at-a-time interview to sharpen a spec or plan
argument-hint: "[topic or spec file, or nothing to grill the current task]"
---
Use the `grilling` skill on: $ARGUMENTS (or the current task if empty).
One question at a time, recommended answer attached to each, facts looked up yourself, decisions put to me.
Persist resolved decisions into `specs/<name>.md` as we go. Do not build anything until I confirm the spec is done.
